<%= form_for [@commenter, @new_comment], :remote => true do |f| %>

<div><%= "comment-#{comment.id}" %></div>

<hr>

<%= link_to "x", @comment, :method => :delete, :remote => true, :confirm => "Are you sure you want to remove this comment?", :disable_with => "x", :class => 'close' %>

<h4><%= comment.user.username %></h4>
	<%= comment.updated_at %>
<p><%= comment.body %></p>

<% end %>